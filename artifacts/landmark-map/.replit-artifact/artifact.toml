kind = "web"
previewPath = "/"
title = "Local Landmarks Map"
version = "1.0.0"
id = "artifacts/landmark-map"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22248

[services.development]
run = "pnpm --filter @workspace/landmark-map run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/landmark-map", "run", "build" ]
publicDir = "artifacts/landmark-map/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22248"
BASE_PATH = "/"
